# CMD Runner 历史上下文

## 2026-06-24 修复：拖入 cmdrunner 运行 bat 时端口号每次递增的问题

### 问题描述
- 用户拖 `A:\3\codex\ue5\ue5_screenshot_tool\start.bat` 进 cmdrunner 运行后，
  反复 ▶/⏹，UE5 截图服务的端口号会从 8765 → 8766 → 8767 不断递增；
  而直接双击 start.bat 则始终是 8765。

### 根因
1. `start.bat` 启动 `python screenshot_server.py`，python 才是真正监听端口的进程。
2. `config.ini` 里 `auto_port = true`，端口被占用会自动切到下一个空闲端口。
3. cmdrunner 原 `stop_tab()` 只调用了 `child.kill()` 杀 cmd.exe，
   它的子进程 python.exe 变成孤儿进程，仍然占着 8765。
4. 下一次点 ▶ 时 8765 已被自家残留 python 占着，于是 auto_port 切到 8766…

### 修复
- `Cargo.toml`：windows-sys 加入 `Win32_System_JobObjects`、`Win32_Security`、`Win32_System_Threading` features。
- `src/main.rs`：
  1. `BatTab` 增加 `child_pid: Option<u32>` 字段，保存 cmd.exe 的 PID。
  2. 新增全局 `JOB_HANDLE: AtomicIsize` 和 `init_job_object()`，
     使用 `CreateJobObjectW` + `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` 创建作业对象，
     `main()` 启动时调用一次。
  3. `run_tab()` 在 `Command::spawn()` 后立即调用 `assign_to_job(child_handle)`，
     把 cmd.exe 及其后续子孙进程全部纳入作业对象。
     额外加 `CREATE_BREAKAWAY_FROM_JOB (0x00000200)`，避免被外层 IDE 的 Job 限制。
  4. `stop_tab()` 改为：先 `taskkill /F /T /PID <pid>` 递归终止整棵进程树，
     再 `child.kill() + wait()` 回收 Zombie；双保险。
  5. `poll_all()` 正常完成时也 `wait()` 子进程并清空 `child_pid`。
  6. cmdrunner 进程异常退出时，OS 关闭 Job 句柄会触发 `KILL_ON_JOB_CLOSE`，
     连带杀掉所有 cmd.exe + python.exe，杜绝孤儿进程。

### 变更文件
- `a:\3\MC\cmdrunner\Cargo.toml`
- `a:\3\MC\cmdrunner\src\main.rs`

### 编译产物
- `a:\3\MC\cmdrunner\target\release\cmdrunner.exe` （约 24.6 MB）

### 验证方式
1. 关掉所有残留 python.exe：`taskkill /F /IM python.exe`
2. 启动新编译的 `cmdrunner.exe`，拖入 `start.bat`
3. 反复 ▶ / ⏹ 多次，观察输出端口号应当始终是 8765
4. 验证残留进程：`netstat -ano | findstr 876` 应该只有 1 个或 0 个 8765

## 2026-06-24 修复：Flask 启动横幅缺失（stderr 没显示）

### 现象
- 拖入 start.bat 后，输出只到 `No UE5 window found yet. Start UE5 and the server will detect it.`
  然后停住，看不到 Flask 的 `* Serving Flask app 'screenshot_server' ... Press CTRL+C to quit` 这一段。
- 但 8765 端口其实早已 LISTENING，浏览器访问 `http://127.0.0.1:8765/ui` 也能正常返回 200。

### 根因
- Flask 的开发服务器横幅、werkzeug 日志默认全部写到 **stderr**。
- 原 `run_tab()` 的读取线程是串行的：
  `for line in stdout.lines() {}` 阻塞等 stdout EOF
  →  Flask 一直在跑，stdout 永远不结束
  →  后面那段 `for line in stderr.lines() {}` 永远轮不到。
- 结果就是 stderr 看似"丢失"。

### 修复
- `src/main.rs` 的 `run_tab()`：把 stdout 和 stderr 拆成**两个独立的线程**，
  并行写入同一个 `Arc<Mutex<String>>`。
- 使用 `Arc<AtomicUsize>` 计数器（初值 2），任一线程结束就 `fetch_sub(1)`，
  归零时才把 `done_flag` 置 true。这样保证两个流都关闭后才标记任务完成。

### 编译产物
- `a:\3\MC\cmdrunner\target\release\cmdrunner.exe` （2026-06-24 14:52）

## 2026-06-24 32 位版编译 + 目录清理

### 新增 32 位编译能力
- 安装 i686-pc-windows-msvc target（绕开清华镜像 404，手动下载 rust-std-1.95.0 解压到工具链目录）。
- 命令：`cargo build --release --target i686-pc-windows-msvc --target-dir target32`
- 产物：
  - 64 位：`target/release/cmdrunner.exe`           (24.66 MB, PE Machine 0x8664)
  - 32 位：`target/release/cmdrunner_x86_32.exe`    (23.07 MB, PE Machine 0x014c)
- 两份 exe 均备份到 `%USERPROFILE%\Desktop\cmdrunner_release\`。

### 目录清理
- `cargo clean`              释放 1.1 GiB（target/）
- `cargo clean --target-dir target32`  释放 562.9 MiB（target32/）
- .gitignore 新增：`/target32`、`/dist`。
- 工具链（rust-std-i686）保留，下次再编 32 位无需重新下载。